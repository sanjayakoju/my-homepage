<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
</head>
<body>
<h1>Shopping Cart</h1>
<% if (Object.keys(cart).length === 0) { %>
    <p>Your cart is empty.</p>
<% } else { %>
    <ul>
        <% for (let key in cart) { %>
            <li>
                <strong><%= cart[key].name %></strong>:
                $<%= (cart[key].price / cart[key].quantity).toFixed(2) %> x <%= cart[key].quantity %> =
                $<%= cart[key].price.toFixed(2) %>
            </li>
        <% } %>
    </ul>
<% } %>
<a href="/">Back to shop</a>
</body>
</html>
